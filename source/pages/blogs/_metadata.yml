# Attach the "blogs" sidebar to every page in this folder (listing + posts),
# so the section navigation persists when a reader opens an individual post.
# Mirrors recipes/_metadata.yml.
sidebar: blogs
